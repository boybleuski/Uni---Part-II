LinkedList
	constructors:
		empty object / provided string
	functions:
		void add(string) - takes a string, then adds to LinkedList one word at a time (one word )
		int count(value_type) - count how many times a certain word appears
		int length() - output how many words are in a string
		remove(value_type) - remove the selected words (value_type) from LinkedList
	notes:
		overload += (concatenation) - concatenate inputOne and inputTwo, then store in inputOne
		overload << (output) - easily outputs LinkedList for viewing
		overload >> (input) - simplifies input into LinkedList

LinkedListDemo
	variables: 	
		ListedList inputOne - sentence to be processed
		ListedList inputTwo - sentence to be appended to inputOne
		
Overloading

Nodes / ListedLists